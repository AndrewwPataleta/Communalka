<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="viewModel" type="com.communalka.app.presentation.ui.main.ProfileViewModel"/>
        <variable name="position" type="Integer"/>
        <variable name="model" type="com.communalka.app.data.model.User"/>
    </data>

<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/placementRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:onClick="@{() -> viewModel.selectSwitchUserByPosition(position)}"
    android:paddingTop="16dp">


    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profileImage"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:src="@drawable/ic_profile"
        android:background="@drawable/circle_gray_background"
        android:padding="16dp"
        android:tint="@color/gray_dark"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/profileImage"
        app:layout_constraintTop_toTopOf="@id/profileImage"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:textColor="@color/black"
        android:textSize="15sp"
        android:text="@{model.name}"

        app:layout_constraintBottom_toBottomOf="@id/profileImage"
        app:layout_constraintVertical_bias="0.5"
        app:layout_constraintHorizontal_bias="0.0"
        android:layout_marginStart="16dp"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
        android:id="@+id/currentUser"
        android:layout_width="18dp"
        android:layout_height="18dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="8dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/profileImage"
        app:layout_constraintBottom_toBottomOf="@id/profileImage"
        app:layout_constraintVertical_bias="0.5"
        android:src="@drawable/ic_subtract"/>

    <View
        android:id="@+id/profileBottomDecorator"
        android:layout_width="0dp"
        android:layout_height="0.6dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/profileImage"
        android:layout_marginTop="16dp"
        android:background="@color/gray_dark"/>




</androidx.constraintlayout.widget.ConstraintLayout>
</layout>